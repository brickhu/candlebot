
# CLAUDE.md — Candlebot 项目上下文

> 供 Claude Code 使用。每次新会话自动读取此文件恢复完整上下文。

---

## 项目概览

| 字段 | 内容 |
|------|------|
| 产品名 | **CandleBot**（中文名：K线专家） |
| 定位 | 一个专门用与K线分析的AI工具，用户通过浏览器扩展/手动上传K线图表，获得AI分析和解读以及操作指引 |
| 核心流程 | 通过chrome扩展截图/手工上传截图 → 后端 AI 视觉分析 → 存储报告 → 网页端展示报告 → 一键分享 |
| 开发者 | fei |
| 域名 | candlebot.app |
| github仓库 | https://github.com/brickhu/candlebot |

---

## 技术栈

### 前端 (Web App)

- **框架**：Solid.js + Vite
- **样式**：TailwindCSS 4.X （dark theme，自定义色板） + DaisyUI
- **语言**：JavaScript
- **部署**：通过github触发vercel实现自动化集成部署
- **生产环境域名**：https://chat.candelbot.app

### 扩展（Chrome 扩展）

- **框架**：Solid.js + Vite
- **样式**：TailwindCSS 4.X （dark theme，自定义色板）
- **构建插件**：`@crxjs/vite-plugin`
- **语言**：JavaScript

### 官网 （使用Solidjs开发Github Pages的静态网页）

- **框架**：Solid.js + Vite
- **样式**：TailwindCSS 4.X （dark theme，自定义色板）
- **语言**：JavaScript
- **生产环境域名**：https://candelbot.app

### 后端

- **框架**：FastAPI（Python）
- **部署**：Railway（已上线）
- **AI 模型**：Minimax MiniMax-Text-01，Anthropic 兼容 API 格式（`x-api-key` 头）, 保留其他大模型的配置；
- **限流**：每 IP 每天 5 次免费（内存存储，重启清零）
- **生产环境域名**：https://api.candelbot.app

---

## 仓库结构

```
candlebot/
├── backend/       # 后端工程，存放所有后端工程代码
├── app/           # web端应用工程，存放所有前端工程代码，通过Vercel自动化部署
├── extension/     # 浏览器插件，插件虽然是独立工程，但它其实是一个配合app使用的工具，存放所有浏览器插件代码
├── website/       # 应用官网，存放官网代码，通过Github Pages自动化部署
└── .gitignore
└── .github/       # github自动化部署相关配置
```

---

## 部署信息

| 项目 | 工程目录 | git分支 | 地址 |
|------|------|------|------|
| 后端 | `backend/` | `main` | `https://api.candelbot.app` |
| 后端 | `backend/` | `dev` | `https://api.candelbot.app` |
| Web App | `app/` | `dev` | `http://localhost:5173` |
| Web App | `app/` | `main` | `http://chat.candlebot.app` |
| 官网 | `website/` | `dev` | `http://localhost:5174` |
| 官网 | `website/` | `main` | `http://chat.candlebot.app` |

---

## 编码规范

- 语言：**默认中文沟通，代码和注释用英文**
- 前端：JavaScript（不使用 TypeScript）
- 样式：只用 Tailwind utility class，不写自定义 CSS（除 index.css 基础重置）
- 组件：Solid.js 函数组件 + Signals，不用 React/Vue
- 后端：Python，Pydantic 校验请求体，httpx 异步请求 AI API
- 不引入非必要依赖，保持扩展体积小

---

## 业务构成

- 前端: 项目工程位于`/app`目录，是用户主要的交互入口；
- 浏览器扩展：项目工程位于`/extension`目录，用户使用它截图，并将截图提交到到web app的用于提交新的K线分析，他的本质是web app的一个便捷提交入口，以及web app其它功能的快速入口；
- 后端: 项目工程位于`/backend`目录，响应前端分析需求，与大模型交互，以及用户管理，订单管理以及其他交易类数据的获取，存储等，他是整个项目的大脑；
- 官网: 项目工程位于`/website`目录，官网的目的是展示和获客，提供插件下载；